# Kodflow Development Container
# Uses pre-built base image from GHCR (includes all tools + Claude Code)
# Languages are added via devcontainer features as needed

FROM ghcr.io/kodflow/devcontainer:latest

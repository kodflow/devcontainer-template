# Exclude everything by default
*

# Include only what's needed for the build
!Dockerfile
!docker-compose.yml
!devcontainer.json

# Include all kodflow scripts
!kodflow/
!kodflow/**

# Include all features
!features/
!features/**

# Exclude sensitive files even if in kodflow/
kodflow/.env
kodflow/mcp.json
.env
mcp.json

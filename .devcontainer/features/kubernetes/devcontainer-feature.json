{
  "id": "kubernetes",
  "version": "1.0.0",
  "name": "Kubernetes Development Environment (kind)",
  "description": "Installs kind, kubectl, and Helm for local Kubernetes development",
  "documentationURL": "https://kind.sigs.k8s.io/",
  "options": {
    "kindVersion": {
      "type": "string",
      "default": "0.27.0",
      "description": "kind version to install (e.g., 0.27.0 or latest)"
    },
    "kubectlVersion": {
      "type": "string",
      "default": "1.32.3",
      "description": "kubectl version to install (e.g., 1.32.3 or latest)"
    },
    "helmVersion": {
      "type": "string",
      "default": "3.17.3",
      "description": "Helm version to install (e.g., 3.17.3 or latest)"
    },
    "enableHelm": {
      "type": "boolean",
      "default": true,
      "description": "Install Helm package manager"
    },
    "enableRegistry": {
      "type": "boolean",
      "default": true,
      "description": "Configure local Docker registry for kind"
    },
    "clusterName": {
      "type": "string",
      "default": "dev",
      "description": "Default kind cluster name"
    }
  },
  "containerEnv": {
    "KUBECONFIG": "/home/vscode/.kube/config",
    "KIND_EXPERIMENTAL_PROVIDER": "docker"
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/docker-outside-of-docker"
  ],
  "privileged": false
}

{
  "id": "claude",
  "version": "2.0.1",
  "name": "Claude Code Integration",
  "description": "Claude Code with the kodflow marketplace plugins (skills, agents, lifecycle hooks), quality scripts, MCP and status line",
  "documentationURL": "https://github.com/kodflow/devcontainer-template",
  "options": {
    "installClaude": {
      "type": "boolean",
      "default": true,
      "description": "Install Claude Code CLI"
    },
    "bundle": {
      "type": "string",
      "enum": [
        "minimal",
        "standard",
        "full"
      ],
      "default": "full",
      "description": "Configuration bundle to install"
    },
    "statusline": {
      "type": "boolean",
      "default": true,
      "description": "Enable custom statusline"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}
